{
  "$comment": [
    "The public surface, classified. Read by scripts/verify-api-stability.mjs, which fails when a",
    "barrel re-exports from a module listed in neither tier — so a new module cannot enter the API",
    "without someone deciding what it promises. A new export inside an already-classified module",
    "inherits that module's tier, which is the right default: adding `getRectArea` to geometry.ts",
    "does not make it experimental.",
    "",
    "stable: covered by semver. A breaking change to any of these is called out under a Changed or",
    "Removed heading in CHANGELOG.md.",
    "",
    "experimental: may change or be removed in any release. Each entry states which of four reasons",
    "applies, and each reason is a fact about the repo rather than a feeling about the code:",
    "  - unobserved: shipped, typechecked, gated, never watched running.",
    "  - off-by-default: behind a policy prop no default configuration turns on.",
    "  - unconsumed: nothing outside its own test file calls it.",
    "  - r3f-canary: reachable only through @infinite-canvas/react/scene, whose @react-three/fiber",
    "    peer range admits a v10 canary. The framework cannot promise stability across someone",
    "    else's canary.",
    "",
    "Purity is NOT a stability reason and size is NOT a stability reason. scene-layer-geometry,",
    "scene-model, spatial-target, and window-proxy all sound like 3D and are all pure-core roots",
    "(verify-pure-core.mjs proves it) with real consumers, so they are stable. window-scene-shell",
    "is equally pure and is experimental, because grepping the tree for its fifteen names finds",
    "only its own test.",
    "",
    "typesExperimental: types.ts is a grab-bag — it holds InfiniteCanvasSceneLayer next to",
    "InfiniteCanvasRect. Its experimental names are listed one by one and checked to still exist."
  ],

  "stable": [
    "camera-navigation",
    "canvas-hud",
    "commands",
    "constants",
    "data-attributes",
    "drop-interaction",
    "factory",
    "geometry",
    "group-layout",
    "group-state",
    "group-tree",
    "history",
    "icons",
    "infinite-canvas",
    "input-policy",
    "keyboard",
    "persistence",
    "portal",
    "recipes",
    "registry",
    "scene-layer-geometry",
    "scene-model",
    "selection",
    "spatial-target",
    "state",
    "store",
    "types",
    "validation",
    "window-focus",
    "window-placement",
    "window-presence",
    "window-proxy"
  ],

  "experimental": {
    "canvas-handle": "unobserved — a debug and automation seam, not a consumer API. Its shape follows whatever drives it.",
    "diagnostics": "off-by-default — this policy exists to switch on the frustum probe layer, and nothing switches it on.",
    "minimap": "unobserved — landed 2026-07-08. No minimap has been drawn in a browser.",
    "offscreen": "unobserved — landed 2026-07-08. No arrow has been drawn in a browser.",
    "rasterization-layer": "off-by-default — the policy, scheduler, and snapshot capture ship behind the rasterization prop. P7 intends to rebuild the capture lane on html-in-canvas.",
    "visibility": "off-by-default — reads the store only the frustum probe layer writes, so every reading is null unless /scene is mounted with diagnostics.frustum.",
    "window-scene-shell": "unconsumed — fifteen exported names, and grepping the tree finds callers only inside window-scene-shell.test.ts. getMinimumWorldLength has no reference anywhere at all. An API nobody has used is an API nobody has found the flaws in.",
    "scene-surface": "r3f-canary — the surface contract the /scene entry implements.",
    "scene:scene-surface": "r3f-canary — the /scene entry in full.",
    "scene:visibility-probes": "r3f-canary — the /scene entry in full.",
    "scene:webgpu-surface": "r3f-canary — the /scene entry in full."
  },

  "typesExperimental": [
    "InfiniteCanvasSceneLayer",
    "InfiniteCanvasSceneLayerFrameloop",
    "InfiniteCanvasSceneLayerPlacement",
    "InfiniteCanvasSceneLayerRenderContext",
    "InfiniteCanvasSceneLayerSpace",
    "InfiniteCanvasSceneVector3"
  ]
}
